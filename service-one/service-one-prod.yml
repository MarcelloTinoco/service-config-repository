spring:
  application:
    name: service-one

server:
  port: 9090

eureka:
  instance:
    hostname: localhost
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_LIST}

#eureka.client.serviceUrl.defaultZone=http://${eureka.instance.hostname}:${server.port}/eureka/,${ADDITIONAL_EUREKA_SERVER_LIST}
#production - ff0cc005c59231bd8f9edfe5cf30d9ab0204b3a3023af7e98bd606aa3f6a227a
app:
 environment: '{cipher}ff0cc005c59231bd8f9edfe5cf30d9ab0204b3a3023af7e98bd606aa3f6a227a'